# Source-derived figures

These figures were extracted from the supplied ActiveTrust PDF and cropped to remove embedded captions before insertion into the LaTeX package. They are preserved as source-derived presentation assets, not as figures regenerated from the public GitHub repository.

The final manuscript should replace them with figures generated by `scripts/run_pipeline.py` once the actual raw per-run archive and figure-generation inputs are available.
